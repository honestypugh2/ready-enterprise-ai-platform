{
    "name": "reap-deck",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "description": "Beyond the Agent — 25-minute technical session. Runs entirely offline after install; a deck that depends on a conference network is a deck that fails on stage.",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "reveal.js": "^5.2.1"
    },
    "devDependencies": {
        "vite": "^8.2.2"
    }
}