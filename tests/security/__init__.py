"""Security suites.

Each file here corresponds to a claim that is easy to make in a slide and hard
to keep in a system: untrusted content cannot act, entitlements are applied at
retrieval rather than at rendering, and telemetry does not become the leak.
"""
